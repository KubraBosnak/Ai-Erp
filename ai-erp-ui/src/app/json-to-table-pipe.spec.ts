import { JsonToTablePipe } from './json-to-table-pipe';

describe('JsonToTablePipe', () => {
  it('create an instance', () => {
    const pipe = new JsonToTablePipe();
    expect(pipe).toBeTruthy();
  });
});
